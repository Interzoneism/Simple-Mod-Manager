<UserControl
    x:Class="VintageStoryModManager.Views.MultiSelectDropdown"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:VintageStoryModManager.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="32"
    d:DesignWidth="160"
    mc:Ignorable="d">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility" />
    </UserControl.Resources>

    <Grid>
        <ToggleButton
            x:Name="DropdownToggle"
            Padding="10,4"
            HorizontalContentAlignment="Stretch"
            Background="{DynamicResource Brush.ComboBox.Background.Normal}"
            BorderBrush="{DynamicResource Brush.Button.Bevel.Dark}"
            BorderThickness="0,0,1,1"
            Click="DropdownToggle_Click"
            ClickMode="Press"
            Focusable="False"
            Foreground="{DynamicResource Brush.Text.Primary}">
            <ToggleButton.Template>
                <ControlTemplate TargetType="ToggleButton">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="30" />
                        </Grid.ColumnDefinitions>
                        <Border
                            x:Name="BackgroundBorder"
                            Grid.ColumnSpan="2"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                            <Grid Margin="{TemplateBinding Padding}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock
                                    x:Name="DisplayText"
                                    Grid.Column="0"
                                    VerticalAlignment="Center"
                                    Foreground="{DynamicResource Brush.Text.Primary}"
                                    Text="{Binding DisplayText, RelativeSource={RelativeSource AncestorType=UserControl}}"
                                    TextTrimming="CharacterEllipsis" />

                                <Path
                                    x:Name="Arrow"
                                    Grid.Column="1"
                                    Width="12"
                                    Height="6"
                                    HorizontalAlignment="Center"
                                    VerticalAlignment="Center"
                                    Data="M 0 0 L 1 0 L 0.5 1 Z"
                                    Fill="{DynamicResource Brush.ComboBox.Glyph}"
                                    RenderTransformOrigin="0.5,0.5"
                                    Stretch="Fill">
                                    <Path.RenderTransform>
                                        <RotateTransform Angle="0" />
                                    </Path.RenderTransform>
                                </Path>
                            </Grid>
                        </Border>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{DynamicResource Brush.ComboBox.Background.Hover}" />
                            <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{DynamicResource Brush.Button.Bevel.Dark}" />
                        </Trigger>
                        <Trigger Property="IsChecked" Value="True">
                            <Setter TargetName="BackgroundBorder" Property="BorderBrush" Value="{DynamicResource Brush.Button.Bevel.Dark}" />
                            <Setter TargetName="BackgroundBorder" Property="Background" Value="{DynamicResource Brush.Button.Primary.Background.Normal}" />
                            <Setter TargetName="Arrow" Property="RenderTransform">
                                <Setter.Value>
                                    <RotateTransform Angle="180" />
                                </Setter.Value>
                            </Setter>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </ToggleButton.Template>
        </ToggleButton>

        <Popup
            x:Name="DropdownPopup"
            AllowsTransparency="True"
            IsOpen="{Binding IsChecked, ElementName=DropdownToggle, Mode=TwoWay}"
            Placement="Bottom"
            PlacementTarget="{Binding ElementName=DropdownToggle}"
            PopupAnimation="Slide"
            StaysOpen="True">
            <Border
                MinWidth="{Binding ActualWidth, ElementName=DropdownToggle}"
                MaxHeight="200"
                Margin="0,4,0,0"
                Padding="4"
                Background="{DynamicResource Brush.ComboBox.Popup.Background}"
                BorderBrush="{DynamicResource Brush.ComboBox.Popup.Border}"
                BorderThickness="1"
                CornerRadius="8">
                <ScrollViewer HorizontalScrollBarVisibility="Disabled" VerticalScrollBarVisibility="Auto">
                    <ItemsControl x:Name="ItemsList">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border
                                    Padding="8,6"
                                    Background="Transparent"
                                    Cursor="Hand"
                                    MouseLeftButtonDown="Item_Click">
                                    <Border.Style>
                                        <Style TargetType="Border">
                                            <Style.Triggers>
                                                <Trigger Property="IsMouseOver" Value="True">
                                                    <Setter Property="Background" Value="{DynamicResource Brush.ComboBox.Item.Background.Hover}" />
                                                </Trigger>
                                            </Style.Triggers>
                                        </Style>
                                    </Border.Style>
                                    <Grid>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="Auto" />
                                            <ColumnDefinition Width="*" />
                                        </Grid.ColumnDefinitions>

                                        <Border
                                            Grid.Column="0"
                                            Width="16"
                                            Height="16"
                                            Margin="0,0,8,0"
                                            BorderBrush="{DynamicResource Brush.Border.Default}"
                                            BorderThickness="1"
                                            CornerRadius="2">
                                            <TextBlock
                                                HorizontalAlignment="Center"
                                                VerticalAlignment="Center"
                                                FontSize="10"
                                                Foreground="{DynamicResource Brush.Accent.Primary}"
                                                Text="✓"
                                                Visibility="{Binding IsSelected, Converter={StaticResource BoolToVisibility}}" />
                                        </Border>

                                        <TextBlock
                                            Grid.Column="1"
                                            VerticalAlignment="Center"
                                            Foreground="{DynamicResource Brush.Text.Primary}"
                                            Text="{Binding DisplayText}" />
                                    </Grid>
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>
            </Border>
        </Popup>
    </Grid>
</UserControl>
