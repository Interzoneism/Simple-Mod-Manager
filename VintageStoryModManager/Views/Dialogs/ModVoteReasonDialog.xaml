<Window
        x:Class="VintageStoryModManager.Views.Dialogs.ModVoteReasonDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Share more details"
        WindowStartupLocation="CenterOwner"
        Height="220"
        Width="480"
        MinHeight="220"
        MinWidth="420"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="24"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="TitleTextBlock"
                   Grid.Row="0"
                   FontSize="18"
                   FontWeight="Bold"
                   TextWrapping="Wrap" />

        <TextBlock x:Name="DescriptionTextBlock"
                   Grid.Row="1"
                   Margin="0,6,0,0"
                   TextWrapping="Wrap"
                   Foreground="{DynamicResource Brush.Text.Subtle}" />

        <ComboBox x:Name="ReasonComboBox"
                  Grid.Row="2"
                  Margin="0,16,0,0"
                  Style="{StaticResource IMM.ComboBoxStyle}"
                  SelectionChanged="ReasonComboBox_OnSelectionChanged" />

        <StackPanel Grid.Row="3"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,20,0,0">
            <Button Content="Cancel"
                    Margin="0,0,12,0"
                    IsCancel="True"
                    Click="CancelButton_OnClick" />
            <Button x:Name="SubmitButton"
                    Content="Submit"
                    IsDefault="True"
                    IsEnabled="False"
                    Click="SubmitButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
