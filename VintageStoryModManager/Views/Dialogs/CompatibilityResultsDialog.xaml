<Window x:Class="VintageStoryModManager.Views.Dialogs.CompatibilityResultsDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Mod Compatibility Results"
        SizeToContent="WidthAndHeight"
        MinWidth="420"
        MinHeight="220"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="20"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image x:Name="IconImage"
               Width="48"
               Height="48"
               Margin="0,0,16,0"
               VerticalAlignment="Top"
               Visibility="Collapsed" />

        <ScrollViewer Grid.Column="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="360"
                      Background="{DynamicResource Brush.Dialog.TextBox.Background}">
            <TextBlock x:Name="MessageTextBlock"
                       TextWrapping="Wrap"
                       MaxWidth="520"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.Text.Primary}" />
        </ScrollViewer>

        <StackPanel Grid.Row="1"
                    Grid.ColumnSpan="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Content="OK"
                    MinWidth="88"
                    IsDefault="True"
                    IsCancel="True"
                    Click="OkButton_OnClick" />
        </StackPanel>
    </Grid>
</Window>
