<Window x:Class="VintageStoryModManager.Views.Dialogs.ExperimentalCompReviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Experimental Compatibility Review"
        Width="760"
        Height="560"
        MinWidth="640"
        MinHeight="480"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="24"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,12"
                   TextWrapping="Wrap"
                   FontSize="14">
            Enter a Vintage Story Mod DB slug (for example, <Bold>carryon</Bold>). The latest Vintage Story version is optional and boosts exact matches.
        </TextBlock>

        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Grid.Column="0"
                       Margin="0,0,16,8"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Text="Mod slug" />
            <TextBox x:Name="ModSlugTextBox"
                     Grid.Row="0"
                     Grid.Column="1"
                     MinWidth="320"
                     Margin="0,0,0,8"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     Background="{DynamicResource Brush.Dialog.TextBox.Background}"
                     BorderBrush="{DynamicResource Brush.Dialog.TextBox.Border}"
                     SelectionBrush="{DynamicResource Brush.Dialog.TextBox.SelectionBackground}"
                     SelectionTextBrush="{DynamicResource Brush.Dialog.TextBox.SelectionForeground}"
                     CaretBrush="{DynamicResource Brush.Dialog.TextBox.Caret}" />

            <TextBlock Grid.Row="1"
                       Grid.Column="0"
                       Margin="0,0,16,0"
                       VerticalAlignment="Center"
                       FontWeight="Bold"
                       Text="Latest VS version" />
            <TextBox x:Name="LatestVersionTextBox"
                     Grid.Row="1"
                     Grid.Column="1"
                     MinWidth="200"
                     VerticalAlignment="Center"
                     HorizontalAlignment="Stretch"
                     Background="{DynamicResource Brush.Dialog.TextBox.Background}"
                     BorderBrush="{DynamicResource Brush.Dialog.TextBox.Border}"
                     SelectionBrush="{DynamicResource Brush.Dialog.TextBox.SelectionBackground}"
                     SelectionTextBrush="{DynamicResource Brush.Dialog.TextBox.SelectionForeground}"
                     CaretBrush="{DynamicResource Brush.Dialog.TextBox.Caret}" />
        </Grid>

        <Border Grid.Row="2"
                BorderBrush="{DynamicResource Brush.Panel.Secondary.Background.Solid}"
                BorderThickness="1"
                CornerRadius="4"
                Padding="12"
                Background="{DynamicResource Brush.Panel.Secondary.Background.Solid}">
            <ScrollViewer VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Auto"
                          Background="{DynamicResource Brush.Panel.Secondary.Background.Solid}">
                <TextBox x:Name="ResultsTextBox"
                         FontFamily="Consolas"
                         FontSize="13"
                         AcceptsReturn="True"
                         AcceptsTab="True"
                         TextWrapping="NoWrap"
                         IsReadOnly="True"
                         BorderThickness="0"
                         Background="Transparent"
                         Foreground="{DynamicResource Brush.Text.Primary}" />
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="StatusTextBlock"
                       Grid.Column="0"
                       VerticalAlignment="Center"
                       Foreground="{DynamicResource Brush.Text.Emphasis}"
                       Text="Ready." />

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="RunButton"
                        Width="120"
                        Margin="0,0,12,0"
                        Content="Run review"
                        IsDefault="True"
                        Click="RunButton_OnClick" />
                <Button x:Name="CopyJsonButton"
                        Width="120"
                        Margin="0,0,12,0"
                        Content="Copy JSON"
                        IsEnabled="False"
                        Click="CopyJsonButton_OnClick" />
                <Button Width="120"
                        Content="Close"
                        IsCancel="True" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>