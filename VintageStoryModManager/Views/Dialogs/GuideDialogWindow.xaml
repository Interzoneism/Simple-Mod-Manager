<Window x:Class="VintageStoryModManager.Views.Dialogs.GuideDialogWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Simple VS Manager Guide"
        Width="720"
        MinWidth="640"
        SizeToContent="Height"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False"
        WindowStyle="SingleBorderWindow"
        Background="{DynamicResource {x:Static SystemColors.WindowBrushKey}}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ScrollViewer Grid.Row="0"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      MaxHeight="420">
            <StackPanel>
                <TextBlock FontSize="20"
                           FontWeight="Bold"
                           Text="Simple VS Manager info" />

                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15">
                    <Run Text="• The manager saves automatic backups before big actions. Restore them from" />
                    <Bold>File &gt; Restore backup point</Bold>
                    <Run Text="whenever you need to roll back." />
                </TextBlock>

                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15">
                    <Run Text="• Use" />
                    <Bold>File &gt; Set Custom Vintage Story Shortcut</Bold>
                    <Run Text="to launch the game with any command-line arguments stored on that shortcut." />
                </TextBlock>

                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15">
                    <Run Text="• Switch off" />
                    <Bold>View &gt; ModDB Design</Bold>
                    <Run Text="to get the tag-sorting list view while browsing the Mod DB." />
                </TextBlock>

                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15">
                    <Run Text="• Want more Mod DB results? Edit"/>
                    <Bold>modDatabaseSearchResultLimit</Bold>
                    <Run Text="(default 30) in " />
                    <Bold>SimpleVSManagerConfiguration.json</Bold>
                    <Run Text="inside the" />
                    <Hyperlink x:Name="ConfigurationFileHyperlink"
                               RequestNavigate="OnHyperlinkRequestNavigate">
                        <Run Text="Simple VS Manager folder"/>
                    </Hyperlink>
                    <Run Text="." />
                </TextBlock>

                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15">
                    <Run Text="• All manager data lives under the" />
                    <Hyperlink x:Name="ManagerFolderHyperlink"
                               RequestNavigate="OnHyperlinkRequestNavigate">
                        <Run Text="Simple VS Manager folder" />
                    </Hyperlink>
                    <Run Text="- configs, caches, and presets." />
                </TextBlock>
                <TextBlock Margin="0,12,0,0"
                           TextWrapping="Wrap"
                           FontSize="15"><Run Text="• " /><Run Text=" "/><Bold><Run Text="Mods &gt; Cache all versions locally"/></Bold><Run Text=" "/><Run Text="is on by default so downloads stay ready. Most mods are small, so the " /><Run Text=" "/><Hyperlink x:Name="CachedModsHyperlink"
                               RequestNavigate="OnHyperlinkRequestNavigate"><Run Text="Cached Mods folder" /></Hyperlink><Run Text=" "/><Run Text=" saves re-download time." /></TextBlock>
            </StackPanel>
        </ScrollViewer>

        <StackPanel Grid.Row="1"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,24,0,0">
            <Button Width="100"
                    IsDefault="True"
                    Content="Close"
                    Click="OnCloseButtonClick" />
        </StackPanel>
    </Grid>
</Window>
