<Window x:Class="VintageStoryModManager.Views.Dialogs.ChangeManagerFolderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Change Manager Config and Cache Folder"
        SizeToContent="WidthAndHeight"
        MinWidth="500"
        MaxWidth="600"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="20"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock x:Name="MessageTextBlock"
                   Grid.Row="0"
                   Margin="0,0,0,20"
                   TextWrapping="Wrap"
                   LineHeight="20" />

        <Grid Grid.Row="1" Margin="0,8,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            
            <Button x:Name="ResetButton"
                    Content="Reset"
                    MinWidth="88"
                    Grid.Column="0"
                    HorizontalAlignment="Left"
                    Click="ResetButton_OnClick" />
            
            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <Button x:Name="YesButton"
                        Content="Yes"
                        MinWidth="88"
                        Margin="0,0,12,0"
                        IsDefault="True"
                        Click="YesButton_OnClick" />
                <Button x:Name="NoButton"
                        Content="No"
                        MinWidth="88"
                        IsCancel="True"
                        Click="NoButton_OnClick" />
            </StackPanel>
        </Grid>
    </Grid>
</Window>
