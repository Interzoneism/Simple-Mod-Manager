<Window x:Class="VintageStoryModManager.Views.Dialogs.VintageStoryVersionSelectionDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Select Vintage Story Version"
        SizeToContent="WidthAndHeight"
        MinWidth="360"
        MinHeight="240"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        ShowInTaskbar="False"
        Background="{DynamicResource Brush.Window.Shell.Background.Solid}">
    <Grid Margin="20"
          Background="{DynamicResource Brush.Panel.Primary.Background.Solid}"
          TextElement.Foreground="{DynamicResource Brush.Text.Primary}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Margin="0,0,0,12"
                   Text="Select the Vintage Story version to check mod compatibility against."
                   TextWrapping="Wrap"
                   FontSize="14" />

        <ListBox x:Name="VersionsListBox"
                 Grid.Row="1"
                 MinWidth="300"
                 MaxHeight="260"
                 SelectionMode="Single"
                 MouseDoubleClick="VersionsListBox_OnMouseDoubleClick"
                 SelectionChanged="VersionsListBox_OnSelectionChanged"
                 DisplayMemberPath="Display"
                 Background="{DynamicResource Brush.Panel.Secondary.Background.Solid}"
                 Foreground="{DynamicResource Brush.Text.Primary}"
                 BorderBrush="{DynamicResource Brush.Panel.Secondary.Background.Solid}" />

        <StackPanel Grid.Row="2"
                    Orientation="Horizontal"
                    HorizontalAlignment="Right"
                    Margin="0,18,0,0">
            <Button x:Name="SelectButton"
                    Content="Select"
                    MinWidth="88"
                    Margin="0,0,12,0"
                    IsDefault="True"
                    Click="SelectButton_OnClick" />
            <Button Content="Cancel"
                    MinWidth="88"
                    IsCancel="True" />
        </StackPanel>
    </Grid>
</Window>