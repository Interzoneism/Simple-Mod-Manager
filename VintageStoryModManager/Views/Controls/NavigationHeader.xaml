<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             x:Class="VintageStoryModManager.Views.Controls.NavigationHeader"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="160" d:DesignWidth="1200">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/MainWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- Navigation and Search Header -->
    <Grid Margin="0,-31,0,0" Height="160" VerticalAlignment="Top">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" MinWidth="428" />
            <ColumnDefinition Width="392" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!-- Tab Navigation Buttons -->
        <StackPanel Grid.Column="0"
                    Orientation="Horizontal"
                    VerticalAlignment="Top" 
                    Height="40" 
                    Margin="0,30,0,0" 
                    Grid.ColumnSpan="2"
                    HorizontalAlignment="Left" 
                    Width="444">
            <Button Content="Installed Mods"
                    Command="{Binding DataContext.ShowInstalledModsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Width="138"
                    FontSize="16"
                    Style="{StaticResource IMM.InstalledTabButtonStyle}" 
                    MinHeight="40" />
            <Button x:Name="ModDatabaseTabButton"
                    Content="Mod DB"
                    Command="{Binding DataContext.ShowModDatabaseCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Width="138"
                    FontSize="16"
                    Style="{StaticResource IMM.ModDatabaseTabButtonStyle}" 
                    Margin="5,0,0,0" 
                    MinHeight="40" />
            <Button x:Name="ModlistsTabButton"
                    Content="Modlists"
                    Command="{Binding DataContext.ShowCloudModlistsCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Width="138"
                    FontSize="16"
                    Style="{StaticResource IMM.ModlistsTabButtonStyle}" 
                    Margin="5,0,0,0" 
                    MinHeight="40" />
        </StackPanel>

        <!-- Search Box -->
        <Grid Grid.Column="2"
              HorizontalAlignment="Right"
              VerticalAlignment="Top" 
              Height="64" 
              Width="251">
            <Grid VerticalAlignment="Center" Height="64" Margin="-253,0,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition />
                    <ColumnDefinition Width="Auto" MinWidth="229" />
                </Grid.ColumnDefinitions>

                <TextBox Grid.Column="1" 
                         MinWidth="200"
                         Text="{Binding DataContext.SearchText, UpdateSourceTrigger=PropertyChanged, RelativeSource={RelativeSource AncestorType=UserControl}}"
                         FontSize="14" 
                         VerticalContentAlignment="Center" 
                         Margin="5,28,0,4">
                    <TextBox.Style>
                        <Style TargetType="{x:Type TextBox}" BasedOn="{StaticResource IMM.SearchTextBoxStyle}">
                            <Setter Property="ui:ControlHelper.PlaceholderText" Value="Search installed mods" />
                            <Setter Property="Visibility" Value="Visible" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.SearchModDatabase, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="ui:ControlHelper.PlaceholderText" Value="Search Mod Database" />
                                    <Setter Property="Visibility" Value="Visible" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding DataContext.IsViewingCloudModlists, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                    <Setter Property="IsEnabled" Value="False" />
                                    <Setter Property="Visibility" Value="Collapsed" />
                                    <Setter Property="ui:ControlHelper.PlaceholderText" Value="Cloud modlist search coming soon" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBox.Style>
                </TextBox>

                <StackPanel Orientation="Horizontal" 
                           Margin="0,28,5,0" 
                           VerticalAlignment="Top" 
                           Height="32"
                           HorizontalAlignment="Right">
                    <Button Content="Refresh"
                            Width="65"
                            Margin="0,0,10,0"
                            ToolTip="Quickly refreshes the current view using cached data if no changes are detected on the online mod page."
                            Background="{DynamicResource Brush.Button.Primary.Background.Normal}"
                            BorderBrush="{DynamicResource Brush.Panel.Secondary.Background.Solid}"
                            Command="{Binding DataContext.RefreshModsUiCommand, RelativeSource={RelativeSource AncestorType=UserControl}}"
                            MinHeight="32">
                        <Button.Style>
                            <Style TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
                                <Setter Property="Visibility" Value="Collapsed" />
                                <Setter Property="Padding" Value="6,6,6,6" />
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding DataContext.IsViewingInstalledMods, RelativeSource={RelativeSource AncestorType=UserControl}}" Value="True">
                                        <Setter Property="Visibility" Value="Visible" />
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </StackPanel>
            </Grid>
        </Grid>
    </Grid>
</UserControl>
