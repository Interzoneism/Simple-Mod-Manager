<UserControl xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:sys="clr-namespace:System;assembly=mscorlib"
             xmlns:viewModels="clr-namespace:VintageStoryModManager.ViewModels"
             x:Class="VintageStoryModManager.Views.Controls.InstalledModsView"
             x:Name="Root"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="1200">
    
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/Resources/MainWindowStyles.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>

    <!-- This UserControl wraps the Installed Mods and ModDB DataGrids -->
    <Border x:Name="Installed_and_ModDB"
            Margin="0,0,-29,0"
            Background="Transparent"
            BorderBrush="{DynamicResource Brush.SunkenSurface.Bevel.Light}"
            BorderThickness="0,0,2,2"
            CornerRadius="{Binding Path=(ui:ControlHelper.CornerRadius), ElementName=ModsDataGrid}"
            SnapsToDevicePixels="True"
            Visibility="{Binding IsViewingCloudModlists, Converter={StaticResource IMM.VisibleWhenFalseConverter}}">
        <Border Background="{DynamicResource Brush.SunkenSurface.Background}"
                BorderBrush="{DynamicResource Brush.SunkenSurface.Bevel.Dark}"
                BorderThickness="2,2,0,0"
                CornerRadius="{Binding Path=(ui:ControlHelper.CornerRadius), ElementName=ModsDataGrid}"
                SnapsToDevicePixels="True">
            <Grid>
                <!-- Installed Mods DataGrid - Placeholder for now -->
                <TextBlock Text="InstalledModsView - DataGrid will be extracted here"
                           HorizontalAlignment="Center"
                           VerticalAlignment="Center"
                           FontSize="18"
                           Foreground="{DynamicResource Brush.Text.Primary}" />
            </Grid>
        </Border>
    </Border>
</UserControl>
