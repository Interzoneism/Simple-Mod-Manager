<Project>
  <PropertyGroup>
    <!-- Update AppVersion to bump the application version in all outputs. -->
    <AppVersion>1.6.3</AppVersion>
    <AppFileVersion>$(AppVersion).0</AppFileVersion>
  </PropertyGroup>
</Project>

